## Stimma 1.0.7 is here

Library resilience release.

- Better handling of legacy media in watched folders when those folders go temporarily missing
- External drives and network shares that unmount or lose permissions mid-scan no longer confuse things
- Faster, more reliable library consistency check on startup
- Test and build pipeline improvements

Worth grabbing if your library lives on an external drive or a NAS.
